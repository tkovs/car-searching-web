const Card = (props) => {
  return (
    <div className="three columns">
          <img className="card" alt="alt" src={props.src} />
          
            
      </div>
  );
};

export default Card;
